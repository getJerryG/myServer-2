// 标题状态枚举
export enum TitleStatus {
    ACTIVE = "active",
    INACTIVE = "inactive",
    EXPIRED = "expired"
}

// 标题类型枚举
export enum TitleType {
    SYSTEM = "system",
    ACHIEVEMENT = "achievement",
    EVENT = "event",
    SEASON = "season"
}